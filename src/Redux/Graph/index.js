const initState = {
    "0": {
        folderName: "root",
    },
};
export default function folderReducer (state=initState, action) {
    switch(action.type) {
        default:
            return state;
    }
}